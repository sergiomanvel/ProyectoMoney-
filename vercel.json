{
  "buildCommand": "npm install -g @angular/cli && cd frontend && npm ci && npm run build -- --configuration=production",
  "outputDirectory": "frontend/dist/autoquote-frontend",
  "framework": "angular"
}

